import makeStyles from '@material-ui/core/styles/makeStyles';

export const colorPickerStyles = makeStyles({
  list: {
    width: '92%',
    display: 'flex',
    flexDirection: 'row',
    color: 'white'
  }
});
